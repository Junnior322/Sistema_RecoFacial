[phases.setup]
nixPkgs = ["xorg.libX11", "libGL", "cmake", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn api:app --host 0.0.0.0 --port $PORT"
